namespace Operia.Domain.Enums;

/// <summary>Defines the lifecycle states shared by appointments and the booking register.</summary>
public enum BookingStatus
{
    Booked = 1,
    Completed = 2,
    Cancelled = 3
}
